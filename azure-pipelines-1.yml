parameters:
  - name: x
    displayName: x naam ka parameter hai
    type: number
    default: 10
    values:
      - 10
      - 20
      - 30

stages:
  - stage: 
    displayName: stage1
    jobs:
      - job: 
        steps:
          - powershell: echo ${{ parameters.x }}